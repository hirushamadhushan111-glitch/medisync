// Shared system constants.
// Average minutes a doctor spends per patient — drives every estimated
// wait time in the queue system (booking confirmations, queue lists,
// token emails, live socket updates).
const AVG_CONSULTATION_TIME = 3;

module.exports = { AVG_CONSULTATION_TIME };
